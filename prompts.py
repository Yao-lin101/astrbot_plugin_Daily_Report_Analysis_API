SUMMARY_PROMPT_TEMPLATE = """对话背景：你在本群的昵称是【{bot_nickname}】，特定用户的昵称是【{user_nickname}】。
角色说明（重要）：
- 消息中带有【你】前缀的是你自己的发言；
- 带有【用户】前缀的是特定用户的发言；
- 带有【群友】前缀的是其他群成员的发言。

任务目标：根据你的AI人设，判断以下群聊记录是否包含值得记录的观察、动态或话题，并以严格的 JSON 格式输出结果。
特别要求：只有在你觉得符合人设观察价值时才进行总结。请忽略无意义的水群（如单发表情包、哈哈哈、收到等）。

输出 JSON 格式要求（必须只返回合法的 JSON 对象，不带 Markdown 符号等包裹）：
{{
  "status": "COMPLETED", // 状态枚举：COMPLETED(有价值且话题已结束)、ONGOING(有价值但话题还在继续)、IGNORED(符合人设判断的毫无价值的水群)
  "topics": [
    {{
      "topic": "这里填写一句话话题名称",
      "content": "这里填写总结内容（50字内，以你的人设口吻讲述特定用户的动态）"
    }}
  ],
  "next_start_id": 1050 // 整数。必须提供！你认为下一次总结应该从哪一条消息ID开始截断（通常是当前话题结束后的新话题起点ID，或最后一条消息的ID）
}}

--- 对话记录开始 ---
{dialogue_text}
--- 对话记录结束 ---"""

PRIVATE_SUMMARY_PROMPT_TEMPLATE = """任务目标：以下是你与特定用户的多轮私聊记录，请根据你的AI人设，判断这段对话的核心内容，并提炼成“话题”与“总结”，以便记录到你的日志中。
特别要求：
1. 提取这段多轮对话最核心的意图、探讨的问题或最终结论。
2. 保持你的人设口吻进行描述。

输出 JSON 格式要求（必须只返回合法的 JSON 对象，不带 Markdown 符号等包裹）：
{{
  "topic": "这里填写一句话话题名称",
  "content": "这里填写这段私聊的总结内容（100字内）"
}}

--- 私聊记录开始 ---
{dialogue_text}
--- 私聊记录结束 ---"""

ACTIVE_MSG_CHECK_STATUS_PROMPT = """任务目标：结合当前时间、用户状态及最近对话上下文，判断现在是否是一个合适的时机去主动发消息关怀用户或发起闲聊。

当前系统时间：{current_time}

判断原则：
1. **优先遵守用户意愿与睡眠状态**：
   - 重点检查对话上下文：如果用户刚说过“晚安”、“去睡了”、“困了”等，说明用户正准备进入或已进入睡眠。
   - 结合实时状态：如果用户已进入睡眠状态（如最近 1 小时无活跃状态，且说过晚安），此时应保持安静，**严禁打扰**。
2. **合理设置延迟 (delay_minutes)**：
   - **初次识别睡眠**：当用户刚说完“晚安”时，建议设置 60-120 分钟的延迟，以便后续确认用户是否真的入睡（防止用户只是暂离）。
   - **深度睡眠期**：当确认用户处于深度睡眠（步数为 0 且已过深夜）时，可以设置较长延迟，但为了能捕捉到用户醒来的时刻，**单次延迟严禁超过 300 分钟（5小时）**。
   - **清晨/醒来预判**：如果你判定用户即将醒来（接近其往常起床时间），应减小延迟（如 30-60 分钟），以便第一时间送上早安。
3. **避免过度打扰**：如果刚结束对话不久（< 30分钟），除非有极佳的关怀理由，否则应继续观望。

输出 JSON 格式要求（必须只返回合法的 JSON 对象，不带 Markdown 符号等包裹）：
{{
  "need_message": true,
  "message_type": "chat", // "care" 或 "chat"
  "reason": "详细说明你的判定逻辑，例如：'识别到用户在 00:04 说了晚安，当前 00:30 步数为 0，判定已入睡，设置 120 分钟后再观察'。",
  "delay_minutes": 0 // 整数。必须填写！若决定立即观望请填 0，基于当前系统时间计算。
}}

--- 角色实时状态 ---
{status_data}

--- 最近私聊对话上下文 (重要，请仔细识别其中的晚安、忙碌等关键词) ---
{conversation_context}
"""

ACTIVE_MSG_PREDICT_TIME_PROMPT = """任务目标：根据以下角色的历史活动记录，推测用户今日开始活跃的大致时间。
请分析用户平时的起床时间、开始使用手机/电脑的时间等规律，给出一个合理的观察启动时间。

输出 JSON 格式要求（必须只返回合法的 JSON 对象，不带 Markdown 符号等包裹）：
{{
  "reason": "根据历史记录，用户通常在早上 8 点左右开始活跃",
  "next_check_time": "08:00" // 格式为 HH:MM，24小时制
}}

--- 状态记录开始 ---
{status_data}
--- 状态记录结束 ---"""

ACTIVE_MSG_GENERATE_PROMPT = """任务目标：根据你的人设以及下面的角色实时状态与历史记忆档案，结合当前系统时间，主动给用户发一条消息。

当前系统时间：{current_time}
当前的发送动机是：{reason}

--- 状态与记忆档案 ---
{memory_data}
--- 档案结束 ---

--- 最近的私聊对话上下文 ---
{conversation_context}
--- 上下文结束 ---

【重要字数与风格限制】：
1. 必须非常简短，最多 1-2 句话（尽量在 30 个字以内）。
2. 就像朋友日常聊天随意开场一样，不要长篇大论，不要写成小作文。
3. 最好用一个简单的问候、一个小发现或一个轻松的提问来结尾，目的是“让用户愿意轻松地回复你”。

请直接输出你要发送的消息内容，不要有任何 Markdown 包裹或附加的说明文字。"""
