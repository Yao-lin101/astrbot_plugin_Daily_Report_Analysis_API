SUMMARY_PROMPT_TEMPLATE = """对话背景：你在本群的昵称是【{bot_nickname}】，特定用户的昵称是【{user_nickname}】。
角色说明（重要）：
- 消息中带有【你】前缀的是你自己的发言；
- 带有【用户】前缀的是特定用户的发言；
- 带有【群友】前缀的是其他群成员的发言。

任务目标：根据你的AI人设，判断以下群聊记录是否包含值得记录的观察、动态或话题，并以严格的 JSON 格式输出结果。
特别要求：只有在你觉得符合人设观察价值时才进行总结。请忽略无意义的水群（如单发表情包、哈哈哈、收到等）。

输出 JSON 格式要求（必须只返回合法的 JSON 对象，不带 Markdown 符号等包裹）：
{{
  "status": "COMPLETED", // 状态枚举：COMPLETED(有价值且话题已结束)、ONGOING(有价值但话题还在继续)、IGNORED(符合人设判断的毫无价值的水群)
  "topics": [
    {{
      "topic": "这里填写一句话话题名称",
      "content": "这里填写总结内容（50字内，以你的人设口吻讲述特定用户的动态）"
    }}
  ],
  "next_start_id": 1050 // 整数。必须提供！你认为下一次总结应该从哪一条消息ID开始截断（通常是当前话题结束后的新话题起点ID，或最后一条消息的ID）
}}

--- 对话记录开始 ---
{dialogue_text}
--- 对话记录结束 ---"""

PRIVATE_SUMMARY_PROMPT_TEMPLATE = """任务目标：以下是你与特定用户的多轮私聊记录，请根据你的AI人设，判断这段对话的核心内容，并提炼成“话题”与“总结”，以便记录到你的日志中。
特别要求：
1. 提取这段多轮对话最核心的意图、探讨的问题或最终结论。
2. 保持你的人设口吻进行描述。
3. 请注意消息的时间戳。如果是由你主动发起的消息（即第一条消息为“你”），且用户是在较长时间（如半小时、数小时等）后才进行回复，请在“总结”中明确体现出这一时间差/时间跨度（例如：“主动发送早安关怀，用户两小时后才回复并...”或“主动发起闲聊，用户在三小时后回复...”等），以便在日志中能够清晰体现时差或长跨度互动的特点。

输出 JSON 格式要求（必须只返回合法的 JSON 对象，不带 Markdown 符号等包裹）：
{{
  "topic": "这里填写一句话话题名称",
  "content": "这里填写这段私聊的总结内容（100字内）"
}}

--- 私聊记录开始 ---
{dialogue_text}
--- 私聊记录结束 ---"""

ACTIVE_MSG_CHECK_STATUS_PROMPT = """任务目标：结合当前系统时间、用户实时状态及最近对话上下文，判断现在是否是一个合适的时机去主动发消息关怀用户或发起闲聊。

--- 基础信息 ---
当前系统观察时间：{current_time} (注：这仅是当前的检查时间，并不代表用户在此刻有活动)

角色说明：
- 带有【你】前缀的是你（机器人）之前的发言。
- 带有【用户】前缀的是特定用户的发言。

--- 判断原则 (优先级排序) ---
1. **严禁打扰睡眠**：
   - 仔细检查【最近对话上下文】：如果用户最后几句话发出了要睡眠的信号，且之后没有新的【用户】发言，说明用户已入睡。
   - 观察【角色实时状态】中的时间戳：例如“(5小时前)”或具体的时间。如果步数、APP活跃等数据都是很久以前的，说明用户没有活动。
   - **严禁将“当前系统观察时间”误认为用户的活跃时间。**

2. **识别“已醒”状态**：
   - 只有当状态数据中出现非常新鲜的活动记录（如“1分钟前”、“5分钟前”）且步数有明显且新鲜的增长时，才判定用户可能已醒。
   - 如果用户在 1 小时内有【用户】角色的对话记录，说明用户活跃。

3. **决策逻辑 (need_message)**：
   - 如果你决定**现在**发送消息，请设置 `need_message: true`。
   - 如果你认为应该**继续观望**或用户正在睡觉，**必须**设置 `need_message: false`。
   - **矛盾禁止**：严禁在 reason 中写“建议延迟”但却设置 `need_message: true`。

4. **合理设置延迟 (delay_minutes)**：
   - **默认观望**：如果你认为当前只需保持常规观望（例如：用户可能正在忙、或还不到打扰的时候），请设置 `delay_minutes: 0`。系统将自动采用默认的随机窗口（{min_interval}-{max_interval}分钟）进行下一次检查。
   - **长时休眠/避打扰**：如果你确定用户在未来较长时间内（超过 {max_interval} 分钟）都不会活跃（如：深夜刚去睡觉、明确表示要消失很久等），请设置一个较大的值（如 120-300 分钟）以节约系统开销。
   - **定点/预约提醒**：如果【最近对话上下文】中用户明确提到了未来的某个时间点（如：“14:00再叫我”、“两小时后找我”），请结合【当前系统观察时间】精确计算出分钟差值，并将其设为 `delay_minutes`。

输出 JSON 格式要求：
{{
  "need_message": false, // true: 立即发送; false: 继续观望
  "message_type": "care", // "care"(关怀) 或 "chat"(闲聊)
  "reason": "请详细说明你的判定逻辑，例如：'用户在02:22说晚安，当前状态显示5小时无活动，判定仍在深度睡眠。'",
  "delay_minutes": 60 // 整数。下次观察的等待分钟数。
}}

--- 角色实时状态 (请重点看数据后的括号时间戳) ---
{status_data}

--- 最近对话上下文 (请仔细识别角色标签) ---
{conversation_context}
"""

ACTIVE_MSG_WAKE_PROMPT = """[系统指令] 根据以下发送动机，扮演你的人设主动向用户发起一次对话。

--- 基础信息 ---
当前系统时间：{current_time}
发送动机（决策理由）：{reason} (类型: {message_type})"""
